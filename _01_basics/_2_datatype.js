"use strict"; // treat all JS code as the newer version

// code readability should be high

// alert(4+4) // we are using nodejs, not browser

let name="bhatia"
let rollNum=488
let pass=true
let id;

/* DATATYPES
 number  => 2 to the power 53(something)
 bigint
 string => ""/''
 boolean => true/false
 null => standalone value
 undefined 
 symbol => unique
 */

// objects

console.log(typeof undefined); // undefined
console.log(typeof null); // object
